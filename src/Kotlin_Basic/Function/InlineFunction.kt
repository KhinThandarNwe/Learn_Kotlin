package Kotlin_Basic.Function

class InlineFunction {
    // output မ ပြောင်းဘူး
    // performance ပိုကောင်းတယ်
    // Higher Order function မှာ သုံးတယ်
    // inline keyword ကို သုံး(Main fun မှာ တိုက်ရိုက်ခေါ်ပေးတယ်)
    // noinline(ခဏခဏ မခေါ် ချင်ရင်သုံး)/ crossinline (return ပြန်ဖို့) => retutn@FunctionName
    //Statement တွေ များပြီး code base ကြီး နေရင် inline အစား noinline / crossinline ကိုသုံး
}